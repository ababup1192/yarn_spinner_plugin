/// 公開ファサード。
///
/// エンジン層・対話層が `Yarn` 経由で内部モジュールへアクセスする際の入口。
/// 直接 `Yarn.Loader` 等を `use` しても良いが、ここに集めると見通しが良い。
mod Yarn {
    // 型の再エクスポートはここでは行わず、各サブモジュールから直接 use する想定。
    // このモジュールは将来 helper（例: parse + init を 1 行で行うショートカット）を
    // 加えるための場所。
}
